openapi: 3.0.4
info:
  title: Sample API
  description: Optional multiline or single-line description in [CommonMark](http://commonmark.org/help/) or HTML.
  version: 0.1.9

servers:
  - url: http://192.168.2.227/
    description: Current development server

paths:
  /v1/hosts:
    summary: Represents the hosts where files and folders can be found
    description: Optional extended description in CommonMark or HTML.
    get:
      summary: Returns a list of hosts.
      description: Optional extended description in CommonMark or HTML.
      responses:
        "200": # status code
          description: A JSON array of host attributes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Host"
    put:
    post:
    delete:
  /v1/hosts/{id}:
    summary: Represents a hosts
    description: >
      This resource represents an individual hosts on the network.
      Each host is identified by a numeric `id`.
  /v1/ignores:
  /v1/ignores/{id}:
  /v1/folders:
  /v1/folders/{id}:
  /v1/hosts/{id}/ignores:
  /v1/hosts/{id}/folders:
  /v1/folders/{id}/files:
  /v1/folders/{id}/files/{id}:
  /v1/files:
  /v1/files/{id}:

components:
  schemas:
    Host:
      type: object
      properties:
        id:
          type: integer
          format: int64
        created_at:
        updated_at:
        name:
          type: string
        domain:
          type: string
        ipv4:
        ipv6:
      required:
        - id
        - name
    Ignore:
      type: object
      properties:
    Folder:
      type: object
      properties:
    File:
      type: object
      properties:

